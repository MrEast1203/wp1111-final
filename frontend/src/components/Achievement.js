import React from "react";
import styled from "styled-components";

const AchievementWrapper = styled.div`
  height: 600px;
  border: 2px solid #000;
`;

const Achievement = () => {
  return <AchievementWrapper>成就/其他</AchievementWrapper>;
};

export default Achievement;
